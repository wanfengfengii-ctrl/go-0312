# 会展中心桁架超厚钢板焊缝拘束拆除与受力转换闭环

## 项目目标

建设一个面向大型会展中心桁架节点超厚钢板对接焊缝的单体 Web 系统。系统以“节点—焊缝—焊层—焊道”只追加证据图为核心，依次管理锁定核验、焊材烘干领用、预热、对称分层施焊、层间检查、后热缓冷、外观与超声检测、返修复验、双人复核和终局裁定。后端采用 Go HTTP 服务与嵌入式关系数据库事务持久化，重启后恢复任务代次、焊材账本、设备租约、待重试调用、有效焊道前缀和终局凭据。前端提供真实的节点总览、焊道拓扑与对称前缀、温度时间轴、焊材守恒、缺陷返修以及复核放行页面，通过 HTTP API 连接后端。实现规模限定为至少 4 个业务包、至少 20 个生产 Go 文件，生产 Go 代码控制在 2000—3000 行并以约 2500 行为目标；前端使用 package.json、提交的 lockfile 和锁定版本构建命令实现确定性构建。

## 端到端业务流程

1. 工艺员创建节点焊接任务，选择有效设计与工艺摘要，录入构件节段、母材炉批、板厚、坡口分区、引弧板、衬板、焊材批次、烘干保温程序、测区、检测映射和阈值；锁定操作校验区间覆盖、炉批匹配、拓扑无环及全部依赖，并生成不可变任务代次。
2. 库管员以唯一包装登记整数克焊材，执行烘干、保温、领用、退库、焊入、焊头和损耗记账；领用事务同时创建保温代次、扣减可用质量并获取烘箱或保温筒租约，任一步冲突则整体回滚。
3. 施工人员确认母材和坡口后获取预热器、测温通道及焊机租约；合格预热覆盖建立热处理屏障，随后按照锁定方向、前驱关系和公开对称规则逐道写入打底、填充、盖面及层间清理证据，形成连续且可重算的有效施工前缀。
4. 焊接完成后按逻辑时间记录后热和缓冷序列，计算后热等效时长与冷却速率；时间轴完整后提交外观和超声检测，设备拒绝或超时只产生确定次数的待重试调用，不推进状态。
5. 缺陷或热加工异常依据焊层邻接、共同热影响区、母材炉批和焊材保温代次计算唯一受影响集合；仲裁器保存原缺陷坐标、刨除体积和旧证据，创建新返修代次并要求受影响焊道及检测测区重新覆盖和复验。
6. 全部证据闭合后，两名不同且资格有效的人员分别复核；仲裁器在单写终局屏障内竞争生成“放行”“裂纹风险隔离”或“取消”之一，只有放行凭据允许拆除焊接拘束并进入结构受力转换。

## 核心组件与职责

1. 钢板焊接工艺与材料规则目录：保存版本化设计及工艺摘要、母材炉批与板厚规则、坡口和焊道区间、焊材及热处理程序、检测映射和资格规则，并阻止引用陈旧摘要。
2. 节点焊接任务及焊道聚合：锁定任务快照，维护节点—焊缝—焊层—焊道只追加图、方向与前驱关系，校验无环拓扑、阶段依赖和双侧对称连续前缀。
3. 焊材守恒与资源租约管理器：维护唯一包装的整数克账本、烘干与保温代次、防跨批污染规则，以及预热器、焊机、保温筒、测温通道、气刨位和超声通道的限时互斥租约。
4. 温度焊接及缓冷证据记录器：记录逻辑时间温度、电流、电压、速度、焊道中断、层间清理、后热及缓冷证据，以整数定点算法计算覆盖率、线能量、累计热输入、等效时长和冷却速率。
5. 缺陷返修与终局仲裁器：生成确定的返修复验集合与新代次，隔离迟到旧回执，执行双人复核、返修次数限制以及放行、裂纹风险隔离和取消的单写终局竞争。
6. Go HTTP API 与 Web 页面：提供带操作号的 JSON API、稳定错误响应、任务及证据查询、脚本化设备适配器，并托管确定构建的前端静态资源。

## 领域规则与不变量

1. 所有长度、板厚、钝边、间隙、焊脚、区间端点和缺陷坐标使用整数微米；质量使用整数克；温度、覆盖率、功率、速度、线能量和冷却速率使用明确比例尺的有符号整数定点数。运算必须先检查负值、零除数、退化区间和乘加溢出，并采用规定的向最近值、半数远离零舍入。
2. 坡口分区和焊道区间必须连续覆盖锁定的设计焊缝；除规则明确允许的边界接触外不得交叠。母材炉批、板厚、引弧板和衬板身份必须与构件节段匹配，设计或工艺摘要一经更新，旧草稿不得锁定。
3. 焊道图必须无环。焊道仅能追加到当前任务代次：所有锁定前驱已完成、当前测区温度位于层间窗口、焊材保温代次正确、方向正确、租约有效、暴露及停焊时限未过期且热输入合格时，才能成为有效前缀的一部分。
4. 双侧坡口采用公开序列规则：每轮按侧 A 的序号 n、侧 B 的序号 n 交替；两侧完成数量之差不得超过一，且任一侧均不得跳过本侧序号。层内拓扑附加前驱仍须同时满足，因此有效序列始终是锁定序列的连续前缀。
5. 每个焊材包装只属于一个炉批与规格；同一保温筒同时只能承载一个保温代次。初始质量必须等于库存、领用在外、已焊入、已退回、焊头和批准损耗之和，所有分量非负且全部以整数克记账。
6. 资源租约由资源标识、持有操作、起止逻辑时间和租约版本组成，同一逻辑时间区间不可重叠。过期租约不能提交证据；设备失败仅追加带固定最大重试次数的调用记录，不得伪造测量或提前释放热处理屏障。
7. 返修集合取缺陷直接命中的焊道，并对同焊层邻接、共同热影响区、相同母材炉批和相同焊材保温代次关系做确定闭包；结果按工程区、构件、节点、焊缝、焊层、焊道、测区、代次排序并去重。返修创建新代次，原焊道、缺陷、刨除体积和复验结果永久保留，旧代次迟到回执只记审计事件。
8. 同一操作号与规范化请求内容相同则返回原结果，与不同内容复用则返回 IDEMPOTENCY_CONFLICT。失败事务不得遗留焊材扣减、租约、温度证据、有效前缀、返修边或放行凭据；放行、隔离与取消共享一个不可覆盖的终局版本。

## 数据模型与持久化

1. CatalogRevision：设计版本、工艺摘要版本、生效时间；MaterialRule、BaseMetalHeat、ConsumableBatch、DryingProgram、ThresholdSet、Qualification 分别保存材料、热处理、阈值及人员资格规则。
2. NodeTask 与 TaskGeneration：工程区、构件节段、节点、锁定快照、状态和代次；Weld、GrooveZone、WeldLayer、WeldPass、PassDependency、HeatAffectedAdjacency 表达连续区间及无环证据图。
3. ConsumablePackage、HoldingGeneration 与 MaterialLedgerEntry：记录唯一包装、批次、整数克增减、去向、操作号和守恒快照；ContainerOccupancy 阻止保温筒跨批污染。
4. Resource、Lease 与 DeviceCall：记录资源类型、逻辑时间租期、版本、调用载荷摘要、脚本结果、重试序号及待重试状态，用数据库唯一约束和事务实现互斥。
5. EvidenceEvent：只追加记录坡口确认、预热读数、焊道参数、层间清理、后热、缓冷、外观及超声结果；PassPrefixProjection 和 ThermalBarrierProjection 是可由事件重建并在事务中校验版本的派生投影。
6. Defect、RepairGeneration、RepairMember、GougingRecord 与 RetestResult：保存缺陷等级和整数微米坐标、确定闭包成员、刨除体积、代次、复验覆盖及旧代次迟到回执。
7. Review 与 TerminalVerdict：记录不同人员的独立复核、资格快照、证据摘要、终局类型和唯一放行凭据；IdempotencyRecord 保存操作号、规范化内容哈希、响应和事务状态。

## 公开接口

1. Web 页面包含节点任务列表、锁定向导、焊道拓扑及双侧前缀视图、焊材账本与租约面板、温度和缓冷时间轴、缺陷返修图、双人复核及终局裁定页；页面必须从后端 API 读取和提交真实数据，不得以内置样例替代。
2. POST /api/catalog/revisions、POST /api/tasks、POST /api/tasks/{id}/lock 管理规则和锁定快照；GET /api/tasks/{id} 与 GET /api/tasks/{id}/graph 返回按领域键稳定排序的状态及证据图。
3. POST /api/material/operations 原子执行烘干、保温、领用、退库、焊入、焊头或损耗；POST /api/leases/acquire、POST /api/leases/{id}/renew 提供带版本的限时互斥租约。
4. POST /api/tasks/{id}/evidence 接收坡口、温度、焊道、清理、后热、缓冷及检测证据；POST /api/device-calls/{id}/retry 按逻辑时间和固定次数推进脚本化设备调用。
5. POST /api/tasks/{id}/defects、POST /api/tasks/{id}/repairs、POST /api/tasks/{id}/retests 创建缺陷、返修代次与复验；POST /api/tasks/{id}/reviews 和 POST /api/tasks/{id}/verdicts 执行复核及终局竞争。
6. 所有写接口要求 Operation-Id，成功和失败均返回规范化 JSON。错误包含稳定 code、领域路径、逻辑时间和排序后的 reasons；至少覆盖 STALE_REVISION、HEAT_MISMATCH、INTERVAL_GAP、INTERVAL_OVERLAP、GRAPH_CYCLE、MATERIAL_OVERDRAWN、CONTAINER_CONTAMINATION、LEASE_CONFLICT、LEASE_EXPIRED、PREFIX_VIOLATION、THERMAL_OUT_OF_RANGE、EXPOSURE_EXPIRED、DEVICE_RETRY_PENDING、FIXED_POINT_OVERFLOW、REPAIR_LIMIT、IDEMPOTENCY_CONFLICT 与 TERMINAL_CONFLICT。
7. 前端目录提交 package.json 和 lockfile，使用锁定的 Node 版本范围与 npm ci 后执行单一 build 命令；Go 服务通过 go:embed 托管产物，前端构建不计入 Go 生产代码行数。

## 失败边界

1. 锁定事务在任何炉批错配、区间缺失或交叠、拓扑成环、陈旧摘要或整数校验失败时全部回滚，不产生半锁定任务代次。
2. 焊材扣减、保温代次创建和资源租约获取属于同一数据库事务；并发超领、租约冲突、保温筒污染或幂等冲突会撤销全部写入。
3. 证据写入使用任务代次和投影版本进行乐观并发校验；逻辑时间倒退、设备回执无效、焊道越序、温度或热输入不合格时只返回拒绝原因，不改变有效前缀。
4. 设备拒绝、断连、超时、校准过期或格式错误只创建或更新待重试调用；重启后按调用标识、重试序号和逻辑时间恢复，达到固定上限后进入人工异常而不生成读数。
5. 定点计算在转换、乘法、累加和除法前逐步检查边界，任何溢出、负时长或零分母终止整个事务，不留下局部热证据。
6. 返修闭包生成与新代次创建在一个事务内完成，并以规范化成员摘要保证唯一；超过返修次数限制或并发创建冲突时不产生部分返修边。
7. 终局使用任务唯一行的比较并交换版本和数据库唯一约束；并发放行、隔离、取消只能有一个成功，失败请求返回现有终局且不得覆盖凭据。
8. 数据库提交后响应丢失时，相同 Operation-Id 可安全重放；服务重启通过事件、账本、租约和调用记录重建投影，并将已过期租约标记失效，但不修改历史事件。

## 验收标准

1. 锁定结果完整固定构件与母材、坡口分区、引弧板和衬板、焊材及烘干程序、焊层焊道拓扑、对称次序、测区、检测映射、阈值、停焊规则和任务代次；陈旧摘要、炉批错配、非法区间及环形依赖均以稳定错误码拒绝。
2. 整数微米区间能够确定验证设计焊缝的连续覆盖且无非法交叠；负值、退化区间、零除数和定点乘加溢出均被拒绝，原因按规定领域路径稳定排序。
3. 并发焊材领用、保温代次创建和租约获取保持原子性；唯一包装的烘干、保温、领用、焊入、退回、焊头和损耗始终满足整数克身份与质量守恒，幂等重试复用原结果，内容冲突整体回滚。
4. 坡口确认、预热、打底、填充、盖面、层间清理、后热和缓冷严格按依赖推进；越序、错向、错误焊材代次、过期租约、暴露或停焊超时不能改变确定的双侧连续施工前缀。
5. 预热覆盖率、层间温度、焊接速度、线能量、累计热输入、后热等效时长和冷却速率均由公开整数定点公式得到可精确断言的结果，并正确处理边界、舍入、逻辑时间乱序和溢出。
6. 脚本化设备的拒绝、断连、超时、校准过期和格式错误只形成可在重启后继续的确定重试记录，不生成伪造读数、不推进焊道前缀，也不释放热处理屏障。
7. 缺陷与热加工异常按照公开邻接表、共同热影响区、母材炉批和焊材保温代次生成唯一返修集合；新代次覆盖全部受影响焊道和测区，原焊道、缺陷坐标、刨除体积、复验及迟到回执不可覆盖。
8. 只有焊材守恒、焊道前缀闭合、完整热处理时间轴、检测合格、返修复验完成以及两名不同合格人员复核均满足时才能生成唯一放行凭据；并发放行、裂纹风险隔离和取消只形成一个终局，且重启后保持不变。

## 确定性测试场景

1. 锁定一个虚构会展中心桁架节点，断言构件、炉批、板厚、测区、阈值和拓扑被固化，后续目录修改不改变已锁定代次。
2. 分别提交缺口、非法交叠、退化端点和负整数微米坡口，断言稳定错误码及完全一致的原因排序。
3. 提交含回边的焊道依赖图，断言 GRAPH_CYCLE 且数据库中不存在任务代次或局部依赖。
4. 用同步屏障并发领用同一包装，断言仅可用质量范围内的事务成功，账本整数克总和守恒且无负余额。
5. 在焊材扣减后模拟保温筒跨批冲突，断言扣减、保温代次和租约全部回滚。
6. 竞争获取同一焊机的重叠逻辑时间租约，断言唯一胜者；重启后租约仍可恢复并在边界时刻准确过期。
7. 按公开 A1、B1、A2、B2 序列写入焊道，断言每步前缀；尝试 A2 跳道、连续 A 侧超差或反向焊接均被拒绝。
8. 在层间温度下界、上界及上下界外各写一条证据，精确断言闭区间边界；验证暴露时限和停焊时限在等于阈值时通过、超过一个逻辑时间单位时失败。
9. 使用固定电流、电压、速度和比例尺序列计算线能量及累计热输入，断言舍入结果；再以最大整数触发乘加溢出并确认无局部证据。
10. 脚本化测温设备依次返回超时、格式错误和成功，断言重试序号固定、前两次不推进前缀、重启后第三次可恢复并记录真实读数。
11. 注入跨相邻焊层、共同热影响区和相同保温代次的夹渣缺陷，断言返修闭包成员、排序和去重结果与公开邻接表完全一致。
12. 创建返修新代次后提交旧代次迟到合格回执，断言当前结论不变且审计事件保留；复验只覆盖部分测区时不得闭合返修。
13. 对同一 Operation-Id 重放相同领用内容，断言响应和账本不变；更换整数克数重放时返回 IDEMPOTENCY_CONFLICT。
14. 模拟事务提交前的设备拒绝和证据版本冲突，断言没有遗留焊材扣减、租约、温度证据、返修边或前缀变化。
15. 仅一人复核、两次由同一人复核、资格过期以及热处理时间轴有缺口时分别请求放行，断言均被拒绝。
16. 用同步屏障并发提交放行、裂纹风险隔离和取消，断言仅一个终局成功、其余返回 TERMINAL_CONFLICT，服务重启后终局及凭据保持一致。

## 组件追踪关系

1. 规则目录对应验收 1、2，主要由锁定快照、区间覆盖、炉批匹配、版本陈旧和拓扑成环测试验证。
2. 节点焊接任务及焊道聚合对应验收 1、4，主要由公开依赖图、双侧 A/B 对称前缀、方向和阶段依赖测试验证。
3. 焊材守恒与资源租约管理器对应验收 3，主要由并发超领、跨批污染、租约竞争、幂等冲突和事务回滚测试验证。
4. 温度焊接及缓冷证据记录器对应验收 4、5、6，主要由温度边界、停焊及暴露边界、定点热输入、溢出、设备故障和重启恢复测试验证。
5. 缺陷返修与终局仲裁器对应验收 7、8，主要由缺陷传播、代次隔离、复验覆盖、双人资格和并发终局测试验证。
6. Go HTTP API 与 Web 页面贯穿全部验收，负责稳定错误结构、Operation-Id、确定排序、真实前后端交互以及 package.json 与 lockfile 驱动的可复现构建；公开测试至少拆分为 catalog_lock_test.go、material_lease_test.go、welding_thermal_test.go、repair_verdict_test.go 四个文件并覆盖不少于 12 个独立用例。

## 独特性

项目聚焦于大型会展中心桁架节点超厚钢板对接焊缝的热加工证据图、双侧对称焊道前缀、整数克焊材守恒和拘束拆除终局凭据，不与既有高强螺栓卸撑、支座解锁或其他材料移交项目重复；复杂度自然来自焊道图持久化、热时序定点计算、并发租约、返修代次隔离及单写终局，不引入无关子系统。
